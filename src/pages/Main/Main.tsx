import React from 'react';

import Cards from 'components/Cards/Cards';

const Main: React.FC = () => {
  return (
    <div className="main-cards-wrapper">
      <Cards />
    </div>
  );
};

export default Main;
